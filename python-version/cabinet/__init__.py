"""The cabinet shell: everything that is not a game.

Screen, fonts, sprites, audio, input, per-game high-score files and the shared
menus. A game lives under `games/` and borrows all of it - see `cabinet.game`
for the contract and `games/registry.py` for the list.
"""
